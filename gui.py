import tkinter as tk

root = tk.Tk()

root.title("Ultra Jarvis AI")

root.geometry("400x250")

label = tk.Label(
    root,
    text="JARVIS ONLINE",
    font=("Arial",20)
)

label.pack(pady=90)

root.mainloop()
